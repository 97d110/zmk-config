# nice!view Asteroid Agent Package v12

This package contains the current animation as a GIF, the rendered GIF split into PNG frames, reusable sprite assets, a manifest, and integration guides for a coding agent.

Start with `manifest.json` and `docs/ASSEMBLY_GUIDE.md`.
