name: Build ZMK firmware
on:
  workflow_dispatch:
  pull_request:
    paths-ignore:
      - "keymap-drawer/**"
  push:
    paths-ignore:
      - "keymap-drawer/**"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
